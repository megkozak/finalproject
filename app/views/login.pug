extends layout

block body
  div.jumbotron
    div.container-fluid
      form(id="user name", method="POST", action="/user")
        div.form-group
          label(for="nameInput")
          input(type="text", id="nameInput", placeholder="Enter your user name here", name="name").form-control
          button(type="submit") Submit
