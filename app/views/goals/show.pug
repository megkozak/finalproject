extends ../layout

block body
  div.container
    if !goal
      p No goal found!
    else
      p
        a(href=`/users/show/${goal._user._id}`)= goal._user.name
        h2= goal.title || 'No Title'
        p= goal.body || 'No Body'
        p= goal.date
